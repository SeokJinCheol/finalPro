<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosta.finalproject.dao.RRBoardMapper">

   <select id="SelectALL" resultType="com.kosta.finalproject.vo.RRboardVO">
	select * from RRBoard
   </select>
   
	<insert id="RRboardinsert" parameterType="com.kosta.finalproject.vo.RRboardVO">
		insert into RRBoard(codeNum, title, category, company, startDate, endDate, bill, contents, registerId, img, adress, spotNum) 
		values(rrb_seq.nextval, #{title}, #{category}, #{company}, #{startDate}, #{endDate}, #{bill}, #{contents}, #{registerId}, #{img}, #{adress}, #{spotNum})
	</insert>
	
	<select id="getcontents" resultType="com.kosta.finalproject.vo.RRboardVO">
		select * from rrboard where codenum = #{codeNum}
	</select>

	<insert id="RPboardinsert" parameterType="com.kosta.finalproject.vo.RPboardVO">
		insert into RPBoard(seqNum,codeNum, category, company, startDate, endDate,userstartDate,userendDate, bill, contents, reaquestId,registerId, img, adress, spotNum) 
		values(rpb_seq.nextval,#{codeNum}, #{category}, #{company}, #{startDate}, #{endDate},#{userstartDate},#{userendDate}, #{bill}, #{contents}, #{reaquestId},#{registerId}, #{img}, #{adress}, #{spotNum})
	</insert>
   </mapper>